#!/bin/bash
set -eu

mkdir -p $BOSH_INSTALL_TARGET/bin

# Compile mlock capability checker
pushd mlock
  make maybe
  cp maybe $BOSH_INSTALL_TARGET/bin/canimlock
popd

# Extract OpenBao binary
pushd openbao
  tar -xzf openbao_2.6.0_linux_amd64.tar.gz
  cp -a bao $BOSH_INSTALL_TARGET/bin
popd
